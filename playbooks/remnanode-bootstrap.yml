# Entry-point playbook that applies the common role to all remnanode servers
- name: Bootstrap remnanode hosts
  hosts: project_shield
  become: true
  vars_files:
    - ../group_vars/all/vault.yml

  roles:
    - common
